{
  "name": "crudzaso",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "api": "json-server --watch db.json --port 3000",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3",
    "vite": "^5.0.0"
  }
}